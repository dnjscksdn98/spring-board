<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="member">
	
	<!-- 회원이 존재하는지 검사 -->
	<select id="memberCheck" resultType="String">
		SELECT user_name AS userName
		FROM member
		WHERE user_id = #{userId} AND user_pw = #{userPw}
	</select>
	
	<!-- 해당 아이디와 비번을 가진 회원 정보 추출 -->
	<select id="viewMember" resultType="com.spring_tutorial.board.model.dto.MemberDto">
		SELECT 
			user_id AS userId,
			user_name AS userName,
			user_email AS userEmail,
			user_regdate AS userRegDate
		FROM member
		WHERE user_id = #{userId} AND user_pw = #{userPw}
	</select>
	
	<!-- 회원가입 -->
	<insert id="signup">
		INSERT INTO member(user_id, user_pw, user_name, user_email)
		VALUES(#{userId}, #{userPw}, #{userName}, #{userEmail})
	</insert>

</mapper>